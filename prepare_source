git_src --branch debian/1.10.5-1 https://salsa.debian.org/rfrancoise/libpcap.git

# Edit the control file so it will built aginst the 1443 
# build environment.
sed -i 's/dpkg-dev (>= 1.22.5)/dpkg-dev (>= 1.22.4)/' $dir/src/debian/control

version_suffix="gl0+bp1443"
